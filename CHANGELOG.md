# 更新記錄 CHANGELOG

> 2026 世界盃推估器（離線可安裝 PWA）。完整模型說明見 `世界盃推估器_說明文件.md`。
> 發版提醒：每次改 `index.html`／`世界盃資料.js` 後，記得更新 `sw.js` 的 `CACHE`（如 `wc2026-v4.5`），使用者重開才會換到新版。

## v4.7 — 四強焦點：隊伍狀態卡（最新 · 2026-07-12）

賽事進入四強（法國、西班牙、英格蘭、阿根廷），首頁「🏆 四強」分頁頂端新增 **🔥 四強焦點** 面板，每場對戰把兩隊狀態並排,含使用者要求的四項：

- **① 狀態**：本屆表現＋傷病/氣勢近況。
- **② 歷史教戰**：兩隊交手史（法vs西 38 次西 18-法 13-7 平、近年西 3 連勝；英vs阿 世界盃 5 次英 3-1-1，含 1986 上帝之手/1998 點球/2002 貝克漢復仇）＋各隊大賽 PK 史。
- **③ 近況戰績**：本屆與近一年重點戰績/榮譽。
- **④ 賭盤建議**：ESPN 奪冠/晉級盤口（2026-07-12 快照）＋本模型晉級機率，並標示「盤口 vs 模型」是否一致、冷門價值在哪（附非投注建議免責）。資料新增於 `世界盃資料.js` 的 `SEMI_FOCUS`，`semiFocus()` 用有效 Elo（含場館僑民主場）算晉級機率。
- 分頁標籤 八強→四強、標頭改「四強聚焦版 v4.7」、對陣圖改「四強起」。SW 快取 → `wc2026-v4.7`；jsdom 驗證（四隊/四項/免責/0 錯誤）通過。

## v4.6.2 — 對陣卡改用台灣時間（2026-07-10）

改直式對陣卡時漏掉了時區，卡片只顯示 `KO_INFO` 的美國當地日期（美東晚上＝台灣隔天，會差一天）。修正：

- 對陣卡日期改為 `zoneStr(kick,'Asia/Taipei',1)` → 顯示「🇹🇼 台灣日期（週X）時:分」；有官方開球時間時採用，否則退回官方日期。
- 「今日」金框判定也改用台灣日期（原本用美國日期會早一天亮）。
- 「近期賽事」卡同步：日期改台灣（`_tw`），今日判定一致。
- Service Worker 快取 → `wc2026-v4.6.2`；jsdom 驗證（UTC 7/10 23:30 → 台灣 7/11 07:30）通過。

## v4.6.1 — 對陣圖改直式卡片（手機友善 · 2026-07-10）

原橫向 SVG 掛圖式對陣樹在手機上字太小、要縮放拖曳，且與「逐場推估」清單重複。改為**直式對陣卡**：

- 移除縮放式 SVG 掛圖與 Fit/放大/縮小控制（連同 `bracketHtml`/`applyBkFit`/`zoomBracket`/`fitBracket` 死碼一併清除）。
- 「逐場推估」升格為主對陣圖：每場一張**全寬卡片**，兩隊上下排、右側晉級機率、**綠底標示晉級者**、🔒官方對陣/✓官方賽果；**點任一隊即可改派它晉級**（保留原手動指定功能），已完賽輪次自動收合。
- 新增**冠軍橫幅**（金色）顯示「推估冠軍（最可能路徑）」，並註明與「模擬綜合奪冠率」的差異，消除兩者可能不同的誤會。
- Service Worker 快取 → `wc2026-v4.6.1`；jsdom 端到端驗證（含點隊指定晉級重繪）通過。

## v4.6 — 八強聚焦・版面整併（2026-07-10）

賽事進入八強，把賽前全覽版收斂成聚焦當前輪次的精簡版：

- **分頁 8 → 6**：`模擬 + 差異 + 說明` 三頁合併為單一「📊 分析」分頁（同頁上下捲即可看模擬結果、推估 vs 實際差異、模型說明）。
- **預設進「🏆 八強」分頁**（原「淘汰」改名）；淘汰賽逐場推估中，**已完賽的 32 強／16 強自動收合**成「▸ 點開看歷史」，畫面直接停在八強 QF。
- **對陣圖也砍到八強起**：掛圖式對陣樹由 9 欄（32/16/8/4/決賽…）縮成 5 欄（8強→4強→決賽→4強→8強），只保留 7 個比賽框，高度大減、一頁看完；由 `BR_START` 常數控制起始輪（之後進四強改 3 即可）。
- **原「小組」→「📅 歷史」**：小組賽賽程與最終積分留作歷史紀錄，移除與八強頁重複的「近期賽事」卡。
- **去重複**：對戰頁移除與「說明」重複的大段模型說明，改為一行連到分析頁；說明頁移除已同頁的「前往差異」按鈕。
- **內建新聞真實性查證**：逐則上網核對 22 則內建新聞（`DEF_NEWS`），修正 5 則與真實 2026 世界盃有出入者——日本（遠藤航實為足傷缺陣，非可踢，−22→−26）、烏拉圭（Giménez 實為傷癒任隊長，−8→+3）、比利時（De Bruyne 帶眼傷非健康先發，+6→−3）、加拿大（Davies 淘汰賽因傷未上，+5→−2）、英格蘭（Palmer 因狀態非傷病落選，用詞修正）。其餘查證屬實。並加**一次性遷移**（`NEWS_FIX_V`）：既有使用者的舊存檔載入時，內建新聞會自動刷新為修正版，但**保留使用者的開/關設定與自訂新聞**（jsdom 驗證通過）。
- Service Worker 快取版本 → `wc2026-v4.6`；標題列標示「八強聚焦版 v4.6」。
- 以 jsdom 端到端驗證 6 分頁皆可渲染、三頁確實併入分析、重複段落已清除。

## v4.5.2 — 修正因子改用統計理論校準（2026-06-30）

差異分頁的「修正因子建議」不再是拍腦袋的比例調整，改為三種標準統計方法推導，並加上顯著性檢定：

- **實力差敏感度（eloScale）**：logistic 尺度重校（Platt scaling）＝以<b>對數損失最小化</b>反解 Elo 分母的最大概似值（grid search 300–900）。
- **平手修正係數（drawBoost）**：<b>Dixon–Coles（1997）</b>對角平手修正 ρ，同以對數損失最小化校準到實際平手頻率（1.00–1.60）。
- **單場平均總進球（totalGoals）**：<b>Poisson 率參數的最大概似估計＝樣本平均</b>（Maher 1982）之閉式縮放。
- **顯著性把關**：每項只在偏差<b>超過抽樣誤差（|z|≥1.96，95%）</b>時才建議，否則視為隨機波動不修正，避免過度擬合小樣本雜訊。建議會顯示 z 值與對數損失改善量。
- `predict()` 加可選 opts 覆寫（供校準 grid search，不動全域設定）。
- Service Worker 快取版本 → `wc2026-v4.5.2`。

## v4.5.1 — 淘汰賽補比分與台灣開賽時間（2026-06-30）

- **淘汰賽比分**：一鍵更新時，除了勝者也一併抓官方<b>比分</b>（平手 PK 顯示 PK 晉級者）。淘汰賽逐場推估卡、對陣圖框、近期賽事卡都會顯示真實比分。
- **淘汰賽開賽時間**：抓取每場開賽 UTC 時間，淘汰賽卡與近期賽事卡顯示<b>台灣／美東／歐洲</b>三地時區（原本只有小組賽有）。
- 新增 `koScore`／`koKick` 狀態（隨備份匯出）。※舊版已更新過的使用者，需再按一次「🔄 一鍵更新比分」才會補上比分與時間。
- Service Worker 快取版本 → `wc2026-v4.5.1`。

## v4.5 — 淘汰賽獨立分頁 ＋ 近期賽事 ＋ 差異分析（2026-06-30）

- **📍 近期賽事卡**：「小組賽」分頁頂端新增今天前後（−2～+3 天）的比賽清單，小組賽與淘汰賽統一排序，點任一場直接帶入對戰預測——解決「滑很久才找到近期比賽」。小組賽期間並自動捲動到當天場次（今日場次金框標示）。
- **🏆 淘汰賽獨立分頁**：對陣圖（原在「奪冠模擬」內）移到專屬分頁，並新增 R32→決賽「逐場推估」清單（官方日期／場館、晉級機率含主場·高原·僑民·PK、官方對陣🔒與賽果✓）。「模擬」分頁精簡為純蒙地卡羅冠軍機率表。
- **🎯 差異分析獨立分頁（推估 vs 實際）**：用<b>目前模型設定</b>重算每場<b>賽前</b>推估（刻意排除「賽中動態」避免資料洩漏），對照已輸入的真實結果，專頁討論精準度，內含：①總評（命中率／技巧分／校準口語判讀）②預測 vs 實際差異表（總進球・平手率・強隊勝率的 Δ）③各隊「實際勝點 vs 賽前期望勝點」超出／低於預期排行 ④最意外場次（模型賦予真實結果機率最低）⑤淘汰賽晉級命中率與冷門。
- **🔧 修正因子建議（一鍵套用）**：偵測系統性偏差時自動給可套用調整——總進球規模、平手修正係數、實力差敏感度（強隊偏保守→更陡、過度自信→更平）。樣本 <16 場時只顯示統計、不給數值建議。
- 分頁列重排為 8 頁（對戰／小組／淘汰／球隊／新聞／模擬／差異／說明），標籤精簡並加窄螢幕自適應。
- Service Worker 快取版本 → `wc2026-v4.5`。

## v4.4 — PWA 化 + 公式守恆修正（2026-06-13）

- **可安裝 PWA**：Android／iPhone 可加到主畫面、全螢幕、離線跑；程式生成 App 圖示（icon-192/512、maskable、apple-touch、favicon）。
- **Worker 模擬守恆修正**：勝率 We 夾值 [0.06, 0.94] 後再分配 λ，讓「⚔️對戰預測 / 🏆奪冠模擬 Worker / 單緒備援」三條路徑採同一公式，修正模擬與即時預測分歧。
- 賽中動態 Elo 改用同尺度 600（與預測對齊）。
- GFPG/GAPG 註解修正、文件不實宣稱修正。
- Service Worker 快取版本 → `wc2026-v4.4`。

## v4.2–4.3 — 多代理人審查修正

- 多輪對抗式 AI 審查，找出並修正既有邏輯／資料缺陷（含高風險項）。

## v4.1 — 五點模型精修

- 攻防節奏 λ（att/def 指數推導期望進球）、動態平手修正（實力接近時加成最高）。
- PK 改 70%當前實力 / 20%門將 / 10%PK史（下修小樣本 PK 史權重）。
- 標準模擬次數預設 5000、冠軍信心區間。

## v4.0 — 效能與對陣樹

- 蒙地卡羅改用 Web Worker（背景執行緒＋進度條＋TypedArray）。
- H2H 遞迴同分平局算法（對齊官方小組賽排序）、對戰樹 Fit／縮放控制。
- 毛玻璃 UI 與 Outfit 字型；修復解析黑屏崩潰。

## v3.9 — 資料與程式分離

- 靜態資料抽離成獨立 `世界盃資料.js`（整批更新只改這檔）。
- 設定加匯出／匯入 JSON 備份。

## v3.5–v3.8 — 校準與資料強化

- v3.8 賽程三地時區（台灣／美東／歐洲）。
- v3.7 內建「📊 模型說明」分頁、種子比分合併。
- v3.6 僑民主場效應＋高原係數重校（證據＋對抗驗證定值）。
- v3.5 市場身價支柱（Transfermarkt，對數 z-score 對齊 Elo）。

## v3.0–v3.4 — 官方對陣與模型校準

- v3.4 模型校準：特質係數減半、賽中動態 Elo（K=40）、538 式擾動、敏感度 400→600（修正西阿過度自信）。
- v3.3 開啟即自動更新比分、待審新聞與翻譯入庫保存。
- v3.2 新聞一鍵翻譯成繁中。
- v3.1 新聞一鍵納入（只收有影響值的）。
- v3.0 FIFA 官方淘汰賽對陣樹（M73–M104）取代簡化種子制。

## v2 — 質化因子

- 球員狀態四檔、球隊特質（韌性／團結／中場）、平均年齡、PK 大戰史、高原效應、一鍵更新比分、市場賠率欄、天氣查詢。

## v1 — 初版骨架

- 48 強分組、72 場賽程、Elo＋Poisson 單場預測、蒙地卡羅奪冠模擬、球員缺陣、新聞因素、五大分頁。
